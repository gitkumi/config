# /home/takumi/.config/tmuxinator/rice.yml

name: rice
root: ~/

windows:
  - dashboard:
      layout: 69ab,190x50,0,0{139x50,0,0[139x42,0,0,0,139x7,0,43,1],50x50,140,0[50x7,140,0,2,50x42,140,8,7]}
      # Synchronize all panes of this window, can be enabled before or after the pane commands run.
      # 'before' represents legacy functionality and will be deprecated in a future release, in favour of 'after'
      # synchronize: after
      panes:
        - cmus
        - cava
        - tty-clock
        - clear && ufetch && later